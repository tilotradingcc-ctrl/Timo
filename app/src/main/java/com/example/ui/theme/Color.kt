package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Professional Polish Theme Highlight Colors
val Indigo600 = Color(0xFF4F46E5)    // Brand Primary (Indigo)
val Indigo50 = Color(0xFFEEF2F6)     // Soft Active Pill Background
val Indigo900 = Color(0xFF312E81)    // Insight Card Background

// Print CMYK Accents (refined for extreme visual polish)
val CyanPrint = Color(0xFF0284C7)    // Refined Cyan (Invoices)
val MagentaPrint = Color(0xFFDB2777) // Refined Magenta (Low Stock / Alerts)
val YellowPrint = Color(0xFFEAB308)  // Refined Yellow / Gold (Quotations)
val DarkCharcoal = Color(0xFF0F172A) // Rich Charcoal

// Slate Surface Hierarchy
val LightBg = Color(0xFFF7F9FC)      // Clean slate/light-blue background
val LightSurface = Color(0xFFFFFFFF) // Crisp white card surface
val MutedSlate = Color(0xFF64748B)    // Clean body text gray

// Border & Separator Accents
val BorderColor = Color(0xFFE2E8F0)       // Slate 200 border
val BorderColorLight = Color(0xFFF1F5F9)  // Slate 100 subtle border

// Dark Theme Variants
val DarkBg = Color(0xFF0F172A)       // Deep slate charcoal
val DarkSurface = Color(0xFF1E293B)  // Premium dark surface
val DarkBorderColor = Color(0xFF334155) // Slate 700 border

